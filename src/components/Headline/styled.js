import styled from "styled-components"

export const HeadlineWrapper = styled.section`
  position: relative;
  z-index: 2;
  user-select: none;
  width: 80%;
  margin: 0 auto;
  display: flex;
  flex-direction: column;
  align-items: center;
`

export const HeadlineTitle = styled.h2`
  color: #fff;
  font-size: 5rem;
  font-weight: 700;
  margin-bottom: 1.25rem;
`

export const HeadlineSlogan = styled.p`
  color: #fff;
  font-size: 2rem;
  font-weight: 700;
  margin-bottom: 3rem;
`
